# Avoid som perl warnings
export LC_ALL=C.UTF-8

# Unset PROMPT_COMMAND from steamdeck to remove PS1 definition
[[ $(echo $PROMPT_COMMAND | grep -c -E '^__steamos_prompt_command') -eq 1 ]] && unset PROMPT_COMMAND
